/**
 * The type Header.
 */
export class Header {
    depotsQuantity: number;
    queryStartTime: string;
    query: string;
    queryDepotSource: string;
    producer: string;
    queryEndTime: string;
    queryDepotRegate: string;
    timestamp: string;
}